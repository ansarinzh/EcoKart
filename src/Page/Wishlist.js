import React from 'react';
import {View, Text} from 'react-native';

const Wishlist = () => {
  return (
    <View
      style={{
        display: 'flex',
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>Sorry , dont have any Wish in wishlist</Text>
    </View>
  );
};

export default Wishlist;
